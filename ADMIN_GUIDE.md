# 兑换码管理系统 - 超简单使用指南

## 🎯 这是什么？

一个网页管理后台，让你轻松管理兑换码发货，**不用敲命令，不用记代码**。

---

## 📱 怎么用？（3步搞定）

### 第一步：打开管理后台
部署后访问：`https://你的域名.com/admin`

### 第二步：收到小红书订单后
1. 在"订单号"输入框填写：`XHS20260403001`（小红书订单号）
2. 在"买家信息"输入框填写：`小红书昵称` 或 `备注信息`
3. 点击"分配兑换码"按钮

### 第三步：复制兑换码发给买家
- 页面会显示生成的兑换码：`GOKAKU-XXXX-XXXX`
- 点击"复制"按钮
- 通过小红书私信发给买家

**完成！** 就这么简单。

---

## 📊 页面功能说明

### 顶部统计卡片
- **可用兑换码**：还剩多少个可以发
- **已发货**：已经发给买家但还没使用的
- **已使用**：买家已经在网站上兑换的

### 分配表单
- 输入订单信息，点击按钮，自动分配兑换码
- 如果订单号重复，会提示"该订单已分配过兑换码"

### 最近订单列表
- 显示最近20条订单记录
- 可以随时复制之前的兑换码（如果买家说没收到）

---

## ❓ 常见问题

### Q1: 如果买家说没收到兑换码怎么办？
在"最近订单"列表中找到对应订单，点击"复制"按钮，重新发给买家。

### Q2: 如果不小心关闭了页面，兑换码还在吗？
在！所有数据都保存在 `lib/data/orders.json` 文件中，刷新页面就能看到。

### Q3: 兑换码会重复吗？
不会。系统自动从1000个兑换码中按顺序分配，每个只用一次。

### Q4: 如果兑换码用完了怎么办？
页面会提示"兑换码已用完"，联系我帮你生成新的。

---

## 🔒 安全提示

**重要**：`/admin` 页面任何人都能访问，建议：

1. **不要公开这个链接**，只有你自己知道
2. 部署后可以考虑加密码保护（我可以帮你加）
3. 定期备份 `lib/data/` 文件夹

---

## 📂 数据文件位置

所有数据保存在：
- `C:/Users/Garo/gokaku/lib/data/redeem_codes.json` - 1000个兑换码
- `C:/Users/Garo/gokaku/lib/data/orders.json` - 订单记录

**建议每周备份一次这两个文件！**

---

## 🚀 现在可以做什么？

1. **本地测试**：
   ```bash
   cd C:/Users/Garo/gokaku
   npm run dev
   ```
   然后访问 `http://localhost:3000/admin` 试试

2. **部署上线**：
   ```bash
   vercel --prod
   ```
   部署后访问 `https://你的域名.com/admin`

---

## 💡 关于域名和数据库

### 域名配置（NEXT_PUBLIC_SITE_URL）
- **作用**：生成分享链接、OG图片链接
- **设置**：部署到Vercel后，在环境变量中设置为你的实际域名
- **例如**：`https://gokaku.com` 或 `https://你的域名.vercel.app`

### 数据库（CloudBase）
- **已配置好**：你的环境变量已经包含了CloudBase配置
- **不需要额外设置**：直接部署就能用
- **数据存储**：用户数据、错题、收藏等都存在CloudBase
- **兑换码数据**：存在本地JSON文件（不在数据库）

---

**就这么简单！有问题随时问我 🙌**
