# Gokaku 用户系统设计方案 v1.0

## 🎯 核心目标

### 用户系统要解决的问题

1. **身份识别**：如何识别用户？（不需要注册登录）
2. **数据持久化**：如何保存用户的错题、进度？
3. **个性化内容**：如何根据用户薄弱点推荐？
4. **跨设备同步**：如何在手机/电脑间同步数据？
5. **防止滥用**：如何防止一个人刷多个账号？

---

## 💡 方案：基于 IP + 本地存储的轻量级用户系统

### 核心思路

**不需要注册登录，用 IP + LocalStorage 识别用户**

```
用户访问网站
    ↓
后端：用 IP 识别（脱敏存储）
前端：用 LocalStorage 存储用户ID
    ↓
用户数据存储在 CloudBase
    ↓
跨设备同步：通过"兑换码"关联
```

---

## 🏗️ 技术架构

### 1. 用户识别机制

#### 方式1：IP识别（已实现）
```typescript
// 后端识别
const ip = req.headers.get("x-forwarded-for")?.split(",")[0];
const hashedIP = hashIP(ip); // SHA256脱敏

// 存储到 CloudBase
{
  user_id: hashedIP,
  created_at: "2026-04-05",
  // ...其他数据
}
```

**优点**：
- ✅ 无需注册登录
- ✅ 用户无感知
- ✅ 已经在用（额度、错题都是基于IP）

**缺点**：
- ❌ 换网络就换ID（家里WiFi → 手机流量）
- ❌ 公共WiFi多人共用一个IP
- ❌ 无法跨设备同步

#### 方式2：LocalStorage + 设备ID（推荐）
```typescript
// 前端生成唯一设备ID
const deviceId = localStorage.getItem('gokaku_device_id') || generateUUID();
localStorage.setItem('gokaku_device_id', deviceId);

// 每次请求带上设备ID
fetch('/api/xxx', {
  headers: { 'X-Device-ID': deviceId }
});

// 后端优先用设备ID，其次用IP
const userId = req.headers.get('X-Device-ID') || hashIP(ip);
```

**优点**：
- ✅ 同一设备永久识别
- ✅ 换网络不影响
- ✅ 无需注册登录

**缺点**：
- ❌ 清除浏览器数据会丢失
- ❌ 无法跨设备同步

#### 方式3：兑换码关联（跨设备同步）
```typescript
// 用户兑换码后，绑定设备ID
{
  redeem_code: "GOKAKU-XXXX-XXXX",
  device_ids: ["device-1", "device-2"], // 可以绑定多个设备
  user_data: {
    wrong_questions: [...],
    progress: {...}
  }
}

// 用户在新设备输入兑换码 → 同步数据
```

**优点**：
- ✅ 可以跨设备同步
- ✅ 用户主动触发（输入兑换码）
- ✅ 防止滥用（一个码最多绑定3个设备）

---

## 📊 数据结构设计

### CloudBase 数据库表

#### 1. users 表（用户基础信息）
```json
{
  "_id": "auto_generated",
  "user_id": "device-uuid-xxx", // 设备ID（主键）
  "ip_hash": "sha256_hash", // IP脱敏（备用）
  "redeem_codes": ["GOKAKU-XXXX-XXXX"], // 已兑换的码
  "quota_remaining": 100, // 剩余额度
  "quota_total": 100, // 总额度
  "created_at": "2026-04-05T10:00:00Z",
  "last_active": "2026-04-05T15:30:00Z"
}
```

#### 2. wrong_questions 表（错题记录）
```json
{
  "_id": "auto_generated",
  "user_id": "device-uuid-xxx",
  "question": "题目内容",
  "user_answer": "用户答案",
  "correct_answer": "正确答案",
  "analysis": "AI分析结果",
  "error_patterns": ["语法混淆", "接续错误"],
  "grammar_id": "にもかかわらず",
  "created_at": "2026-04-05T15:30:00Z"
}
```

#### 3. grammar_weakness 表（薄弱点统计）
```json
{
  "_id": "auto_generated",
  "user_id": "device-uuid-xxx",
  "grammar_id": "にもかかわらず",
  "error_count": 3, // 错误次数
  "last_seen": "2026-04-05T15:30:00Z",
  "mastered": false // 是否已掌握
}
```

#### 4. user_progress 表（学习进度）
```json
{
  "_id": "auto_generated",
  "user_id": "device-uuid-xxx",
  "total_questions": 45, // 总做题数
  "correct_count": 32, // 正确数
  "accuracy": 0.71, // 正确率
  "streak_days": 7, // 连续天数
  "last_practice": "2026-04-05",
  "milestone_100": false, // 是否完成100道（返现）
  "updated_at": "2026-04-05T15:30:00Z"
}
```

#### 5. device_sync 表（设备同步）
```json
{
  "_id": "auto_generated",
  "redeem_code": "GOKAKU-XXXX-XXXX",
  "device_ids": ["device-1", "device-2"], // 绑定的设备
  "max_devices": 3, // 最多绑定3个设备
  "created_at": "2026-04-05T10:00:00Z"
}
```

---

## 🎨 前端用户界面设计

### 1. 导航栏添加"我的"按钮

```
[Logo] [工具] [资料] [定价]        [我的] [购买]
```

点击"我的" → 跳转到 `/dashboard`

### 2. 个人中心页面 `/dashboard`

```
┌─────────────────────────────────────┐
│  👤 我的学习中心                      │
├─────────────────────────────────────┤
│                                     │
│  📊 学习统计                         │
│  ├─ 累计做题：45 道                  │
│  ├─ 正确率：71%                      │
│  ├─ 连续天数：7 天                   │
│  └─ 距离返现：还需 55 道 (45/100)    │
│                                     │
│  💪 薄弱语法点                       │
│  ├─ にもかかわらず (错3次) [复习]    │
│  ├─ に対して (错2次) [复习]          │
│  └─ とともに (错2次) [复习]          │
│                                     │
│  📝 最近错题 (查看全部 →)            │
│  ├─ 彼は仕事が忙しい___、毎日...     │
│  └─ この問題は難しくて、私___...     │
│                                     │
│  🎁 我的额度                         │
│  ├─ 剩余：87 次                      │
│  ├─ 已用：13 次                      │
│  └─ [兑换码] [邀请好友]              │
│                                     │
│  🔗 跨设备同步                       │
│  └─ [输入兑换码同步数据]             │
│                                     │
└─────────────────────────────────────┘
```

### 3. 错题本页面 `/wrongbook`（已有）

优化显示：
- 按语法点分类
- 按错误模式分类
- 按时间排序
- 标记"已掌握"

### 4. 进度追踪组件（工具页顶部）

```
┌─────────────────────────────────────┐
│  📈 今日进度：3/20 次                 │
│  🎯 返现进度：45/100 道 (还需55道)    │
│  🔥 连续学习：7 天                    │
└─────────────────────────────────────┘
```

---

## 🔄 跨设备同步流程

### 场景：用户在手机上做了50道题，想在电脑上继续

**步骤1：手机上生成同步码**
```
我的 → 跨设备同步 → 生成同步码
显示：SYNC-ABCD-1234
```

**步骤2：电脑上输入同步码**
```
我的 → 跨设备同步 → 输入同步码
输入：SYNC-ABCD-1234
点击：同步数据
```

**步骤3：数据合并**
```
手机数据：50道错题
电脑数据：10道错题
合并后：60道错题（去重）
```

**技术实现**：
```typescript
// 生成同步码（基于兑换码）
const syncCode = redeemCode; // 复用兑换码

// 绑定设备
await db.collection('device_sync').add({
  redeem_code: syncCode,
  device_ids: [currentDeviceId],
  max_devices: 3
});

// 同步数据
const syncData = await db.collection('device_sync')
  .where({ redeem_code: syncCode })
  .get();

// 合并数据
mergeUserData(localData, syncData);
```

---

## 🎁 邀请系统设计

### 邀请流程

```
用户A → 我的 → 邀请好友 → 生成专属链接
    ↓
分享链接：www.gokaku.cn/tool?ref=INVITE-XXXX
    ↓
用户B点击链接 → 自动记录邀请关系
    ↓
用户B使用工具 → 双方各得10次额外查询
```

### 邀请奖励规则

| 事件 | 邀请人奖励 | 被邀请人奖励 |
|------|-----------|-------------|
| 被邀请人首次使用 | +10次 | +10次 |
| 被邀请人购买 | +20次 | - |
| 邀请满10人 | +100次 | - |

### 技术实现

```typescript
// 生成邀请码
const inviteCode = `INVITE-${generateShortCode()}`;

// 记录邀请关系
await db.collection('invites').add({
  inviter_id: currentUserId,
  invite_code: inviteCode,
  invited_users: [],
  total_rewards: 0,
  created_at: new Date()
});

// 被邀请人访问
const ref = new URLSearchParams(window.location.search).get('ref');
if (ref) {
  localStorage.setItem('invite_ref', ref);
  // 首次使用时触发奖励
}
```

---

## 📱 个性化推荐逻辑

### 1. 薄弱点识别

```typescript
// 从错题中提取薄弱语法点
const weakGrammar = await db.collection('grammar_weakness')
  .where({ user_id: userId })
  .orderBy('error_count', 'desc')
  .limit(5)
  .get();

// 推荐复习内容
const recommendations = weakGrammar.data.map(g => ({
  grammar_id: g.grammar_id,
  error_count: g.error_count,
  priority: calculatePriority(g), // 根据错误次数、时间计算优先级
  review_materials: getReviewMaterials(g.grammar_id)
}));
```

### 2. 学习路径推荐

```typescript
// 根据用户水平推荐学习路径
if (accuracy < 0.6) {
  // 正确率低 → 推荐基础语法
  recommend('基础语法强化');
} else if (accuracy < 0.8) {
  // 正确率中等 → 推荐易混淆语法
  recommend('易混淆语法对比');
} else {
  // 正确率高 → 推荐高级语法
  recommend('高级语法拓展');
}
```

### 3. 考前冲刺清单

```typescript
// 考前7天生成专属清单
const examDate = new Date('2026-07-05');
const daysLeft = Math.ceil((examDate - new Date()) / (1000 * 60 * 60 * 24));

if (daysLeft <= 7) {
  // 生成高风险语法清单
  const highRiskGrammar = weakGrammar.filter(g => 
    g.error_count >= 2 && // 错误2次以上
    g.star >= 2 && // 真题考频2星以上
    !g.mastered // 未掌握
  );
  
  sendNotification('考前冲刺清单已生成，请查看');
}
```

---

## 🚀 实施步骤

### 第1步：修改现有代码（1天）

1. **添加设备ID生成**
```typescript
// lib/device.ts
export function getDeviceId(): string {
  if (typeof window === 'undefined') return '';
  
  let deviceId = localStorage.getItem('gokaku_device_id');
  if (!deviceId) {
    deviceId = `device-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    localStorage.setItem('gokaku_device_id', deviceId);
  }
  return deviceId;
}
```

2. **修改所有API路由**
```typescript
// 从 IP识别 改为 设备ID识别
const deviceId = req.headers.get('X-Device-ID');
const userId = deviceId || hashIP(getClientIP(req));
```

3. **添加导航栏"我的"按钮**
```typescript
// components/Navbar.tsx
<Link href="/dashboard">
  <User className="w-4 h-4" />
  我的
</Link>
```

### 第2步：开发个人中心（2-3天）

1. **创建 `/dashboard` 页面**
   - 学习统计卡片
   - 薄弱点列表
   - 最近错题
   - 额度显示
   - 跨设备同步入口

2. **创建 API 路由**
   - `/api/dashboard/stats` - 获取统计数据
   - `/api/dashboard/weakness` - 获取薄弱点
   - `/api/dashboard/sync` - 跨设备同步

### 第3步：开发返现功能（1天）

1. **进度追踪**
```typescript
// 每次错题分析后更新进度
await db.collection('user_progress').update({
  total_questions: db.command.inc(1),
  correct_count: isCorrect ? db.command.inc(1) : 0
});

// 检查是否达到100道
if (progress.total_questions >= 100 && !progress.milestone_100) {
  // 触发返现：延长会员1个月
  await extendMembership(userId, 30);
  await db.collection('user_progress').update({
    milestone_100: true
  });
  
  // 弹窗庆祝
  return { milestone: true, reward: '¥9.9' };
}
```

2. **前端显示进度条**
```typescript
<div className="progress-bar">
  <div className="progress" style={{ width: `${progress}%` }}>
    {current}/100 道
  </div>
</div>
```

### 第4步：开发邀请系统（1-2天）

1. **生成邀请码**
2. **记录邀请关系**
3. **发放奖励**

---

## 📊 数据监控指标

### 用户行为指标
- 日活跃用户（DAU）
- 周活跃用户（WAU）
- 平均做题数/用户
- 平均正确率
- 100道完成率

### 转化指标
- 免费→付费转化率
- 邀请转化率
- 跨设备同步率

### 留存指标
- 次日留存
- 7日留存
- 30日留存

---

## 💡 关键设计原则

### 1. 无需注册登录
- ✅ 降低使用门槛
- ✅ 用户无感知
- ✅ 数据自动保存

### 2. 数据可同步
- ✅ 通过兑换码跨设备
- ✅ 最多绑定3个设备
- ✅ 防止滥用

### 3. 个性化推荐
- ✅ 基于错题数据
- ✅ 识别薄弱点
- ✅ 生成专属清单

### 4. 激励机制
- ✅ 100道返现
- ✅ 邀请奖励
- ✅ 连续打卡

---

## 🎯 总结

**核心方案**：
- 用设备ID识别用户（无需注册）
- 用兑换码实现跨设备同步
- 用错题数据做个性化推荐
- 用返现机制提高使用深度

**优先级**：
1. **立即做**：添加设备ID、修改API路由
2. **本周做**：开发个人中心、返现功能
3. **下周做**：邀请系统、个性化推荐

**预期效果**：
- 用户数据永久保存
- 跨设备无缝同步
- 个性化学习体验
- 提高用户粘性

---

**最后更新**：2026-04-05  
**制定人**：Claude + Garo
