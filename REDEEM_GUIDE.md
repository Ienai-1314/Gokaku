# 兑换码系统使用指南

## 📦 发货流程

### 方案 A：小红书自动回复（推荐）

1. **设置自动回复**
   - 进入小红书店铺后台
   - 设置"付款后自动回复"
   - 回复内容模板：

```
🎉 感谢购买！以下是你的专属内容：

📚 资料包网盘链接：
[网盘链接]
提取码：[提取码]

🤖 AI工具兑换码：
GOKAKU-XXXX-XXXX

💡 使用方法：
1. 访问 https://gokaku.app/redeem
2. 输入兑换码解锁100次查询额度
3. 前往 https://gokaku.app/tool 开始使用

有问题请联系客服微信：[你的微信号]
```

2. **批量发货**
   - 打开 `lib/data/redeem_codes.txt`
   - 每个订单复制一个兑换码
   - 按顺序发货，避免重复

### 方案 B：手动发货

1. 用户付款后私信你
2. 从 `redeem_codes.txt` 复制一个未使用的码
3. 发送给用户（附带网盘链接）

---

## 📊 兑换码管理

### 查看兑换码状态

打开 `lib/data/redeem_codes.json`，每个码包含：

```json
{
  "code": "GOKAKU-MSZK-FN94",
  "status": "unused",  // unused=未使用, used=已使用
  "createdAt": "2026-04-03T10:30:00.000Z",
  "usedAt": "2026-04-03T12:45:00.000Z",  // 使用时间（仅已使用）
  "usedBy": "123.45.67.89"  // 使用者IP（仅已使用）
}
```

### 生成更多兑换码

如果1000个码不够用：

```bash
cd C:/Users/Garo/gokaku
npx ts-node scripts/generate_codes.ts
```

会自动追加新码到现有文件。

---

## 🔍 查询兑换记录

### CloudBase 数据库

登录腾讯云 CloudBase 控制台，查看以下集合：

1. **user_quota** - 用户额度记录
   - `ip`: 用户IP
   - `remaining`: 剩余次数
   - `total`: 总次数
   - `used`: 已使用次数
   - `redeemCodes`: 已兑换的码列表

2. **redeem_logs** - 兑换日志
   - `code`: 兑换码
   - `ip`: 用户IP
   - `redeemedAt`: 兑换时间
   - `quotaAdded`: 增加的额度（100）

---

## 💰 定价策略

| 商品 | 价格 | 包含内容 | 兑换码 |
|------|------|----------|--------|
| 资料包 | ¥29 | 网盘资料 | ❌ 不需要 |
| AI工具 | ¥19 | 100次查询 | ✅ 需要 |
| 组合价 | ¥39 | 资料+工具 | ✅ 需要 |

**纯利润计算：**
- 资料包：¥29（无成本）
- AI工具：¥19 - API成本约¥2 = ¥17
- 组合价：¥39 - API成本约¥2 = ¥37

---

## ⚠️ 注意事项

1. **兑换码唯一性**
   - 每个码只能使用一次
   - 系统会自动标记为已使用

2. **额度永久有效**
   - 用户兑换后额度不会过期
   - 可以慢慢使用

3. **防止滥用**
   - 兑换码绑定到用户IP
   - 同一IP可以兑换多个码（额度累加）

4. **客服处理**
   - 如果用户反馈兑换失败，检查：
     - 兑换码是否输入正确（大小写不敏感）
     - 是否已被使用（查看 `redeem_codes.json`）
     - CloudBase 数据库是否正常

---

## 🚀 上线前检查

- [ ] CloudBase 创建 `user_quota` 集合
- [ ] CloudBase 创建 `redeem_logs` 集合
- [ ] 上传 `redeem_codes.json` 到服务器
- [ ] 测试兑换流程（使用一个测试码）
- [ ] 设置小红书自动回复
- [ ] 准备网盘资料并上传
- [ ] 更新网盘链接到自动回复模板

---

## 📈 销售数据追踪

定期检查：
1. `redeem_codes.json` 中 `status: "used"` 的数量 = 已售出
2. CloudBase `redeem_logs` 集合 = 兑换记录
3. 小红书后台订单数 = 总销售额

**示例查询（CloudBase 控制台）：**

```javascript
// 查询今日兑换数量
db.collection('redeem_logs')
  .where({
    redeemedAt: db.command.gte('2026-04-03T00:00:00.000Z')
  })
  .count()
```

---

## 🎯 营销建议

1. **限时优惠**
   - "前100名购买送额外50次查询"
   - 手动给这些用户额外发一个兑换码

2. **分享奖励**
   - 用户分享后可获得额外兑换码
   - 从 `redeem_codes.txt` 手动发放

3. **复购优惠**
   - 老用户再次购买打折
   - 或赠送额外额度

---

祝生意兴隆！🎉
