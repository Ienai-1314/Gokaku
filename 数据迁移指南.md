# 数据迁移指南：从旧系统迁移到账号系统

## 📋 迁移概述

本次迁移将现有的基于设备ID/IP的用户识别系统升级为基于账号的系统。

### 迁移内容

1. **创建账号表** (`accounts`)
   - 为每个唯一的 `user_id` 创建一个免费账号
   - 账号ID = 原来的设备ID/IP哈希

2. **创建设备绑定表** (`device_bindings`)
   - 记录设备ID到账号ID的映射关系
   - 支持一个账号绑定多个设备

3. **更新数据字段**
   - 8个集合中的 `user_id` 字段添加对应的 `account_id` 字段
   - 保留原 `user_id` 字段以便回滚

## 🔧 迁移前准备

### 1. 确认环境变量

确保 `.env.local` 文件包含以下配置：

```env
NEXT_PUBLIC_ENV_ID=your-env-id
TENCENT_SECRET_ID=your-secret-id
TENCENT_SECRET_KEY=your-secret-key
```

### 2. 确认数据库集合

确保以下集合已创建：
- ✅ accounts
- ✅ device_bindings
- ✅ users
- ✅ query_history
- ✅ collections
- ✅ wrong_questions
- ✅ weakness_analysis
- ✅ practice_records
- ✅ blessing_records
- ✅ invitations

### 3. 备份数据（重要！）

在腾讯云控制台手动导出所有集合数据：
1. 进入云开发控制台
2. 数据库 → 选择集合 → 导出
3. 保存导出的JSON文件

## 🚀 执行迁移

### 步骤1: 运行迁移脚本

```bash
cd C:/Users/Garo/gokaku
node scripts/migrate-to-account-system.js
```

### 步骤2: 查看迁移日志

脚本会输出详细的迁移过程：

```
🚀 开始数据迁移到账号系统...

📊 收集所有唯一的 user_id...
  ✓ users: 找到 X 条记录
  ✓ query_history: 找到 X 条记录
  ...

✅ 共找到 X 个唯一的 user_id

🔧 创建账号和设备绑定...
  ✓ device-xxx: 创建账号和绑定
  ...

✅ 创建完成: X 个账号, X 个跳过

📦 迁移集合数据...
  ✓ users: X 条更新, X 条跳过
  ...

🔍 验证迁移结果...
  ✓ accounts 表: X 个账号
  ✓ device_bindings 表: X 个绑定
  ...

✅ 数据迁移完成！
```

### 步骤3: 验证迁移结果

在腾讯云控制台检查：

1. **accounts 表**
   - 记录数 = 唯一 user_id 数量
   - 每条记录包含：account_id, account_type, devices, daily_limit 等

2. **device_bindings 表**
   - 记录数 = 唯一 user_id 数量
   - 每条记录包含：device_id, account_id, first_bound_at 等

3. **其他集合**
   - 每条记录都有 `account_id` 字段
   - `account_id` 的值 = 原来的 `user_id`

## 🔍 迁移后检查

### 1. 数据完整性检查

```javascript
// 在腾讯云控制台运行查询
db.collection('query_history').where({
  user_id: db.command.exists(true),
  account_id: db.command.exists(false)
}).count()
// 应该返回 0
```

### 2. 功能测试

- [ ] 访问网站，检查是否能正常使用
- [ ] 查询语法/词汇，检查额度是否正确
- [ ] 查看个人中心，检查账号信息显示
- [ ] 兑换一个测试码，检查是否能创建会员账号
- [ ] 换一个浏览器，输入兑换码，检查是否能同步数据

## ⚠️ 注意事项

### 迁移特点

1. **非破坏性迁移**
   - 保留原 `user_id` 字段
   - 只添加新的 `account_id` 字段
   - 可以随时回滚

2. **向后兼容**
   - 新代码优先使用 `account_id`
   - 如果 `account_id` 不存在，降级到 `user_id`
   - 旧数据仍然可以正常工作

3. **免费账号创建**
   - 所有现有用户都被创建为免费账号
   - 每天3次查询额度
   - 可以通过兑换码升级为会员

### 可能的问题

1. **重复运行脚本**
   - 脚本会检查账号是否已存在
   - 已存在的账号会被跳过
   - 可以安全地重复运行

2. **部分迁移失败**
   - 脚本会继续处理其他记录
   - 失败的记录会在日志中标记
   - 可以手动修复或重新运行

3. **网络超时**
   - 如果数据量大，可能需要分批迁移
   - 可以修改脚本添加分页逻辑

## 🔄 回滚方案

如果迁移后发现问题，可以回滚：

### 方案1: 删除新字段（推荐）

```javascript
// 在腾讯云控制台运行
const collections = ['users', 'query_history', 'collections', ...];

for (const name of collections) {
  db.collection(name).where({
    account_id: db.command.exists(true)
  }).update({
    account_id: db.command.remove()
  });
}
```

### 方案2: 恢复备份

1. 删除所有集合
2. 导入备份的JSON文件
3. 重新部署旧版本代码

## 📊 迁移后优化

### 1. 清理旧字段（可选）

迁移稳定运行一段时间后，可以删除 `user_id` 字段：

```javascript
// 在腾讯云控制台运行
db.collection('query_history').where({
  user_id: db.command.exists(true)
}).update({
  user_id: db.command.remove()
});
```

### 2. 添加索引

为新字段添加索引以提升查询性能：

```javascript
// 在腾讯云控制台创建索引
// accounts 表: account_id (唯一索引)
// device_bindings 表: device_id (唯一索引), account_id (普通索引)
// 其他表: account_id (普通索引)
```

## 📝 迁移记录

| 时间 | 操作 | 结果 | 备注 |
|------|------|------|------|
| 2024-XX-XX | 创建 accounts 和 device_bindings 表 | ✅ | - |
| 2024-XX-XX | 运行迁移脚本 | ✅ | 迁移 X 个账号 |
| 2024-XX-XX | 功能测试 | ✅ | 所有功能正常 |
| 2024-XX-XX | 部署到生产环境 | ✅ | - |

## 🆘 问题排查

### 问题1: 脚本运行失败

**错误**: `Error: env is not defined`

**解决**: 检查 `.env.local` 文件是否存在，环境变量是否正确

---

### 问题2: 账号创建失败

**错误**: `Error: duplicate key`

**解决**: 账号已存在，可以忽略或删除后重新创建

---

### 问题3: 数据不一致

**现象**: 部分记录有 `account_id`，部分没有

**解决**: 重新运行迁移脚本，脚本会自动跳过已迁移的记录

---

## 📞 联系支持

如果遇到无法解决的问题，请联系开发者。
